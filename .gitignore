venv/
env/
__pycache__/
*.pyc
.idea/

# cin-data/ = documents KYC bruts (source), sensibles : jamais versionnés.
# data/kyc/ = dataset train/val dérivé : versionné (voir demande explicite).
# data/externe/ = images Kaggle, régénérables via scripts/download_kaggle.py.
cin-data/
data/externe/
downloads/
uploads/

# Snapshots de ré-entraînement (rollback local) : seul model.keras à la racine est déployé.
models/

logs/
documents.db

Claude outputs/
